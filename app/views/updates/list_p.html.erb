<div class="col container">
    <h2>Products</h2>
    <% @products.each do |product| %>
        <div class="col-md-6"><%= product.pname %></div>
        <div class="col-md-6"><%= link_to "Edit", edit_product_path(product), class: "btn btn-primary" %></div>
    <% end %>
</div>
